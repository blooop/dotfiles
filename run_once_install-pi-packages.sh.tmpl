#!/bin/bash
{{ if .heavy -}}
set -e

if ! command -v pi &> /dev/null; then
    echo "⏭ pi not found, skipping package installation"
    exit 0
fi

echo "⏭ no pi packages configured"
{{ else -}}
# Skipping pi packages installation (profile: {{ .profile }}, no heavy toolchain)
exit 0
{{ end -}}
